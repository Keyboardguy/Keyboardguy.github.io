"use strict"
function deg_to_rad(x) {
	return x * (Math.PI / 180);
}

function rad_to_deg(x) {
	return x * (180 / Math.PI);
} 

function square(x) {
	return x * x;
}

export function fill_in_unknown_forces(data) {
    // TODO
}

